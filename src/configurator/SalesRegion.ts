// The Sales Region offered by the demo as a hard-coded Fixed Decision. Restricting the type to a string union means
// only these values (or the empty "no region" selection) can flow into the state.
export type SalesRegion = "EU" | "US";
